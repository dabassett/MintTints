'use strict';

var jQuery = require('jquery');
var $ = jQuery;
var tinycolor = require('tinycolor');
var bootstrap = require('bootstrap'); // todo pick only what you need

jQuery(function() {
  console.log(bootstrap);
  console.log($);
  console.log(tinycolor);
});
